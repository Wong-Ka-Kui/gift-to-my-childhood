# 宠物乐园 · Garden 部署包

这是 Node 后端项目，请保留后端服务，不能只发布 dist 静态文件。

## 启动

- 安装依赖：`npm ci`
- 重新构建：`npm run build`（包内已有 dist 和 server-runtime，可直接启动）
- 启动命令：`npm start`
- 监听：`0.0.0.0`，读取平台提供的 `PORT`，默认 4173。
- 支持根路径和 `/seed/<项目路径>/`，前端 API 使用项目相对路径。

## 数据

后端将游客资料、模型文件、宠物介绍、头像、家具、金币及照料状态保存到 `GARDEN_DATA` 环境变量指定的持久化目录。请由平台注入并跨版本保留该目录。若未设置，则使用项目内 `.garden-data`，仅适合本地测试，不保证重新部署后保留。

当前为游客模式：只输入名字，不需要密码。浏览器保留 HttpOnly 登录 Cookie，模型和存档在服务器。同名游客不会共用数据。清除 Cookie、换浏览器或换设备会产生新的游客，暂不提供跨设备账号找回；不要将用户名视为密码。

包内已包含作者现有的奶娃、奶龙、奶蛙三个完整模型、头像、原始宠物介绍和家具布置。每个新游客首次进入房间时自动获得这三只宠物，不需要重新导入；仍可再导入一只自己的宠物。初始内容来自 `starter/home.json` 和配套模型文件，各游客的照料和金币进度独立，不包含作者的登录信息、金币或历史进度。已有服务器存档不会被新模板覆盖。开发模式 `npm run dev` 仍使用原来的浏览器本地存档。

模型上传限制：每只宠物 100 MB，最多 100 个文件，每个房间最多 4 只宠物。服务端使用单 Node 进程串行处理存档请求，适用于当前小规模试玩；不要配置多个同时写同一目录的 Node 副本。

## 发布前检查

Garden 上传页说明：zip 上传后会自动部署并发布到广场。此包仅在本地生成、验证，没有上传或发布。正式环境发布需由具备权限的发布负责人执行。

发布负责人应确认持久化目录已挂载，再选择 Node 项目并上传此 zip。上线后检查实际 `/seed/.../` 地址、首次登录、模型上传、刷新恢复、清扫奖励，并在后续版本更新后再次确认旧存档仍在。

## 本地验证

`npm test` 运行游戏规则测试；`node scripts/check-server.mjs` 验证服务端子路径、游客隔离、模型存取、并发保存、清扫奖励和进程重启后恢复。后者使用独立临时目录，不读取真实存档。

打包只包含宠物游戏源码、预构建产物、依赖清单、解码器及角色生成页面；排除 `.git`、`node_modules`、环境密钥、本地存档、`games/` 和 `public/othello/`。
